/*
 *  Create another input file with only the pizzas id (0...n) sorted according to no of I in descending order
 */

// Container for all methods
let modifyInputFile = {};

modifyInputFile.index = (arr, input) => {

    for (i = 0; no_of_I_arr.length; i++) {

    }

}

module.exports = modifyInputFile;
